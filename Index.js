import { registerRootComponent} fromm'expo';
import App from './App';
// registerComponent('main' , () => App);
// It also ensures that whether you load the app in Expo Go or in a native biild,
// the environment is set up appropriately
registerRootCompnent(App);
